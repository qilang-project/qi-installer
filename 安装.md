# 奇语言（qilang）安装

编译产物零运行时依赖；奇语言编译工具链本身运行时需系统 **clang**（macOS 自带 Xcode CLT，Linux `apt install clang`，Windows 需 MSVC/clang）。

## macOS / Linux —— Homebrew

```bash
# 方式一：本地 formula 直接装
brew install --formula qi-installer/brew/qi.rb

# 方式二：建 tap 后（推荐，能 brew upgrade）
brew tap qilang-project/qi https://github.com/qilang-project/homebrew-qi
brew install qi
```
装完直接可用（`qi` 自动找到随包的运行时库，无需设环境变量）：
```bash
qi run 你的程序.qi
```

## Windows —— winget

已上架社区仓（`qilang.qi`，2026-07-28 合并）：

```powershell
winget install qilang.qi

# 本地清单直接装（改了清单想先自测）：
winget install --manifest qi-installer\winget\manifests\q\qilang\qi\<版本>
```

## 手动安装（任意平台，无包管理器）

从 [Releases](https://github.com/qilang-project/qi/releases) 下对应平台包，解压后把 `bin/` 加入 PATH。包布局 `bin/qi` + `lib/qi/`，`qi` 按 `<前缀>/lib/qi/` 自动定位运行时库，解压即用。

## 发新版后更新安装清单

```bash
qi-installer/scripts/更新安装清单.sh <release-tag>   # 自动拉包、算 sha256、回填 brew+winget
```

**winget 线上那份不用管这个脚本** —— 打完 tag 后 `qi` 仓 Release workflow 的 `winget`
job 会自己提更新 PR（见下）。脚本刷出来的 `winget/manifests/` 只是本地 `--manifest`
自测用的副本，以及万一自动化坏了拿来手工提。brew formula 仍然只能靠脚本。

## 上架说明（维护者）

- **Homebrew tap**：把 `brew/qi.rb` 放进 `qilang-project/homebrew-qi` 仓的 `Formula/qi.rb`。
- **winget**：首次上架（`New-Package`）是手工提 PR、要 moderator 审的，
  `qilang.qi` 已经在 2026-07-28 合入。**之后的版本更新已自动化**：
  `qi/.github/workflows/release.yml` 里的 `winget` job 用
  [winget-releaser](https://github.com/vedantmgoyal9/winget-releaser) 从 release
  资产里挑 `*windows-x64.zip`，算 sha256、继承上一版的
  `NestedInstallerType`/`PortableCommandAlias`，推到 `liliang-cn/winget-pkgs`
  fork 后开 PR。更新 PR 不需要 moderator，验证过了一般几十分钟自动合。

  需要的配置（一次性）：`qi` 仓 secret **`WINGET_TOKEN`** = classic PAT，
  只勾 `public_repo`。fine-grained token 不行（要往 fork 推分支）。
  没配的话那个 job 会静静跳过，不影响发布本身。

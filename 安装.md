# 奇语言（qilang）安装

编译产物零运行时依赖；奇语言编译工具链本身运行时需系统 **clang**（macOS 自带 Xcode CLT，Linux `apt install clang`，Windows 需 MSVC/clang）。

## macOS / Linux —— Homebrew

```bash
# 方式一：本地 formula 直接装
brew install --formula qi-installer/brew/qi.rb

# 方式二：建 tap 后（推荐，能 brew upgrade）
brew tap qilang-project/qi https://github.com/qilang-project/homebrew-qi
brew install qi
```
装完直接可用（`qi` 自动找到随包的运行时库，无需设环境变量）：
```bash
qi run 你的程序.qi
```

## Windows —— winget

```powershell
# 上架 winget 社区仓后：
winget install qilang.qi

# 本地清单直接装（提交社区仓前自测）：
winget install --manifest qi-installer\winget\manifests\q\qilang\qi\<版本>
```

## 手动安装（任意平台，无包管理器）

从 [Releases](https://github.com/qilang-project/qi/releases) 下对应平台包，解压后把 `bin/` 加入 PATH。包布局 `bin/qi` + `lib/qi/`，`qi` 按 `<前缀>/lib/qi/` 自动定位运行时库，解压即用。

## 发新版后更新安装清单

```bash
qi-installer/scripts/更新安装清单.sh <release-tag>   # 自动拉包、算 sha256、回填 brew+winget
```

## 上架说明（维护者）

- **Homebrew tap**：把 `brew/qi.rb` 放进 `qilang-project/homebrew-qi` 仓的 `Formula/qi.rb`。
- **winget**：把 `winget/manifests/...` 三个 yaml 提 PR 到 [microsoft/winget-pkgs](https://github.com/microsoft/winget-pkgs)。
